[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "localknowledge-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "numpy",
]

[tool.hatch.build.targets.wheel]
packages = ["src/localknowledge"]
